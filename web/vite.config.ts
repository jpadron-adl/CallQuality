/// <reference types="vitest/config" />
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import { fileURLToPath, URL } from 'node:url';

// Configuración de Vite para el dashboard del profesor.
// - Proxy: redirige /api al servidor HTTP de la API (npm run dev:api en la raíz, puerto 3000),
//   de modo que el frontend habla con rutas relativas y no necesita CORS en desarrollo.
// - Vitest: entorno jsdom con setup de Testing Library para los tests de componentes.
export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:3000',
        changeOrigin: true,
      },
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    css: true,
  },
});
